# Packaging Placeholder

Status: documentation/readiness only.

This file records future packaging constraints for the desktop shell placeholder. It is not a packaging script and does not define a Tauri build.

Current state:

- No installer artifact exists.
- No production Tauri build exists.
- No code signing exists.
- No auto-updater exists.
- No telemetry exists.
- No release workflow exists.
- No public download path exists.

Future packaging work must be planned as a separate slice after fresh Windows VM validation and must preserve LAN/token protection, setup loopback-only behavior, stop-task task-record-only behavior, and disabled connector defaults.
