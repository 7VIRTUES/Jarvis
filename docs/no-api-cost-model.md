# No API Cost Model

Current Jarvis v0.1 scope does not use OpenAI, Anthropic, Gemini, or other paid AI APIs. The only allowed Codex behavior is local CLI availability detection.

External connector manifests can describe future cost modes, but no connector is implemented or enabled in current v0.1 scope.
