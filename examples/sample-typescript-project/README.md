# Sample TypeScript Project

This project exists so Jarvis v0.1A can inspect a small local TypeScript workspace and plan checks from detected package scripts.

## Scripts

```powershell
npm run typecheck
npm run test
```

